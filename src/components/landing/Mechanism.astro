---
/**
 * Edibles-to-tincture comparison:
 * Introduces tinctures from the familiar starting point of edibles without attacking edibles.
 */
import { OILS_IMAGE } from '@/data/landing';

const comparisons = [
  {
    edible: 'Usually comes as a fixed piece.',
    botanica: 'Uses a liquid draw you can repeat visually.',
  },
  {
    edible: 'Usually goes through digestion.',
    botanica: 'Can be held under the tongue or swallowed.',
  },
  {
    edible: 'Changing the amount often means changing the portion.',
    botanica: 'You can choose a smaller or larger visual draw.',
  },
  {
    edible: 'Looks and feels like food.',
    botanica: 'Uses a small, discreet dropper bottle.',
  },
];
---

<section id="mechanism" class="w-full bg-surface">
  <div class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop py-20 md:py-28">
    <div class="grid grid-cols-1 md:grid-cols-12 gap-gutter md:gap-x-16 items-center">
      <div class="md:col-span-6 space-y-stack-md reveal">
        <span class="font-label-sm text-label-sm uppercase tracking-widest text-secondary font-bold">
          The Format
        </span>
        <h2 class="font-headline-lg text-headline-lg text-primary">
          You already know edibles. Here is what changes.
        </h2>
        <p class="font-body-lg text-body-lg text-on-surface-variant leading-relaxed">
          Gummies and brownies give you a familiar fixed portion. A tincture changes the format: liquid, discreet, and adjustable by visual draw.
        </p>
      </div>

      <div class="md:col-span-6 reveal">
        <figure>
          <div class="aspect-[4/3] rounded-xl overflow-hidden botanical-shadow bg-surface-container-low">
            <img
              src={OILS_IMAGE}
              alt="Amber tincture bottle with a glass dropper on a neutral tabletop"
              referrerpolicy="no-referrer"
              loading="lazy"
              decoding="async"
              class="w-full h-full object-cover"
              width="960"
              height="720"
            />
          </div>
          <figcaption class="font-body-sm text-body-sm text-on-surface-variant mt-3">
            Botanica tincture: a discreet liquid format designed for deliberate, repeatable use.
          </figcaption>
        </figure>
      </div>
    </div>

    <div class="mt-14 md:mt-16 reveal-group">
      <div class="hidden sm:grid sm:grid-cols-2 gap-8 pb-3 border-b border-outline-variant/40 px-2 font-label-sm text-xs font-bold uppercase tracking-widest">
        <span class="text-on-surface-variant/70">Edible</span>
        <span class="text-secondary">Botanica</span>
      </div>
      {comparisons.map((item) => (
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 sm:gap-8 py-5 border-b border-outline-variant/40 px-2 items-baseline">
          <div>
            <span class="sm:hidden font-label-sm text-xs uppercase tracking-widest text-on-surface-variant/70 block mb-1 font-bold">Edible:</span>
            <p class="font-body-md text-body-md text-on-surface-variant leading-relaxed">{item.edible}</p>
          </div>
          <div>
            <span class="sm:hidden font-label-sm text-xs uppercase tracking-widest text-secondary block mb-1 font-bold">Botanica:</span>
            <p class="font-body-md text-body-md text-primary font-medium leading-relaxed">{item.botanica}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
